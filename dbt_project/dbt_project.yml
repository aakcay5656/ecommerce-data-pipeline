name: 'ecommerce_transformation'
version: '1.0.0'
config-version: 2

profile: 'dbt_project'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ecommerce_transformation:
    staging:
      materialized: view
      schema: staging
    warehouse:
      materialized: table
      schema: warehouse
    marts:
      materialized: table
      schema: analytics
