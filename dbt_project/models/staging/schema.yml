version: 2

models:
  - name: stg_orders
    description: "Cleaned and typed orders from raw data"
    columns:
      - name: order_id
        description: "Primary key"
        tests:
          - unique
          - not_null
      - name: amount
        description: "Order amount in USD"
        tests:
          - not_null
